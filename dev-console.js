// ==============================================\n// DEVELOPER CONSOLE FOR RUNTIME KEYTIP MANAGEMENT\n// Add this to browser console for quick testing\n// ==============================================\n\n// Usage in browser console:\n// addCustomKeytip(['H', 'X', 'X'], 'My Custom Command')\n// removeCustomKeytip(['H', 'X', 'X'])\n// listAllKeytips()\n\nwindow.keytipsConsole = {\n  // Add a keytip at runtime (for testing)\n  addCustomKeytip: (sequence, label) => {\n    console.log(`🔧 DEV: Adding custom keytip [${sequence.join(' → ')}] → ${label}`);\n    // This would integrate with the dynamic registration system\n    alert(`Would add: ${sequence.join(' → ')} → ${label}`);\n  },\n  \n  // Remove a keytip\n  removeCustomKeytip: (sequence) => {\n    console.log(`🗑️ DEV: Removing keytip [${sequence.join(' → ')}]`);\n    alert(`Would remove: ${sequence.join(' → ')}`);\n  },\n  \n  // List all current keytips\n  listAllKeytips: () => {\n    console.log('📋 Current KeyTips:');\n    console.log('• Alt/Cmd → H → V → V  (Paste Values)');\n    console.log('• Alt/Cmd → H → B → B  (Border Bottom)');\n    console.log('• Alt/Cmd → H → B → T  (Border Top)');\n    console.log('• Alt/Cmd → H → O → I  (AutoFit Column)');\n    console.log('• Alt/Cmd → A → S      (Sort Descending)');\n    console.log('• Alt/Cmd → H → C → C  (Clear Content)');\n  },\n  \n  // Test conflict detection\n  checkConflicts: (sequence) => {\n    const existing = [\n      ['H','V','V'], ['H','B','B'], ['H','B','T'], \n      ['H','O','I'], ['A','S'], ['H','C','C']\n    ];\n    \n    const hasConflict = existing.some(seq => {\n      const seqStr = sequence.join('');\n      const existingStr = seq.join('');\n      return seqStr === existingStr || \n             seqStr.startsWith(existingStr) || \n             existingStr.startsWith(seqStr);\n    });\n    \n    console.log(`🔍 Conflict check for [${sequence.join(' → ')}]:`, \n                hasConflict ? '❌ CONFLICT' : '✅ OK');\n    return !hasConflict;\n  }\n};\n\nconsole.log('🎹 KeyTips Developer Console loaded!');\nconsole.log('Available commands:');\nconsole.log('• keytipsConsole.addCustomKeytip([\"H\", \"X\", \"Y\"], \"My Command\")');\nconsole.log('• keytipsConsole.removeCustomKeytip([\"H\", \"X\", \"Y\"])');\nconsole.log('• keytipsConsole.listAllKeytips()');\nconsole.log('• keytipsConsole.checkConflicts([\"H\", \"X\", \"Y\"])');\n\nexport {}; // Make this a module\n